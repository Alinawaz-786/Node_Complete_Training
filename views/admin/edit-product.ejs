<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/product.css">
</head>
<body>
<%- include('../include/navber.ejs') %>

<main>
    <form class="product-form" action="/admin/update-product" method="post">
        <input type="hidden" value="<%= product.id ? product.id :'' %>" name="id">
        <div class="form-control">
            <label for="title">Title</label>
            <input type="text" value="<%= product.title ? product.title :'' %>" name="title" id="title">
        </div>
        <div class="form-control">
            <label for="imgUrl">imgUrl</label>
            <input type="text" value="<%= product.imageUrl ? product.imageUrl :'' %>" name="imgUrl" id="imgUrl">
        </div>
        <div class="form-control">
            <label for="price">Price</label>
            <input type="number" value="<%= product.price ? product.price :'' %>" name="price" id="price" step="0.01">
        </div>
        <div class="form-control">
            <label for="description">Description</label>
            <textarea type="text" name="description" id="description"><%= product.description ? product.description :'' %></textarea>
        </div>

        <button type="submit">Update</button>
    </form>
</main>

</body>

</html>
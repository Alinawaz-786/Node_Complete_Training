<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <main>

            <%  if(products.length > 0) {%>
                <% products.forEach(element => { %>
                    <li>
                        <%= element.productData.title %>
                            (
                            <%= element.qty %>)
                    </li>
                    <%})%>
                        <%} else{%>
                            <%}%>
        </main>
        <%- include('../includes/end.ejs') %>